name: CI
on:
  pull_request:
  push:
concurrency:
  group: github
  cancel-in-progress: false
jobs:
  linter:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: repo file execution
        run: python3 main.py
